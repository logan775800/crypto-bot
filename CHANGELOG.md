# 更新日志

每次发版在最上面加一段。`/changelog` 看得到，启动播报会带上当前版本这一段。
格式固定：`## vX.Y.Z　(日期)` + 若干 `- ` 条目，handlers/changelog.py 按这个格式解析。

## v1.51.0　(2026-08-26)
- 🐞 **@机器人问币时回「无法调用工具」**：行情工具链挂掉后会**静默降级**成纯聊天，
  而且降级后还用着「你有 12 个工具」的提示词，模型只能去解释自己调不到工具
- ✅ 现在降级会**明说**：「这一轮取不到实时数据，上面只是基于常识的判断」，
  并提示改用发币名 / `/scan` / `/liqmap`（这些不走 AI，不受影响）
- ✅ 降级时换成不提工具的提示词，并**禁止编造价格、涨跌幅、资金费率**这类数字。
  没有数据源时最危险的不是答不上来，是编一个出来
- ❤️ 连续降级会进心跳巡检，管理员会收到通知——以前只在服务器日志里，没人看得到
- 🧹 `/howto` 去掉 GitHub 外链：完整清单现在就在**群置顶**里（68 个命令 + 按钮路径），
  外链等于让人跳出去看一份重复的

## v1.50.1　(2026-08-25)
- 🐞 **LP 撤出告警一直是死的**：我把监控名单当成字典读，实际是扁平列表，
  每 5 分钟抛一次 `AttributeError`，从上线起就没真正跑过
- ❤️ **是心跳巡检发现的**——它就是为这种"安静地失效"存在的，
  这次证明了它有用（换以前只会一直没告警，没人知道）
- 🛡 测试也一起修了：以前 fixture 是我凭印象手写的字典，
  **和代码错在同一个地方所以一路绿灯**。现在改成用真正写数据的函数造，
  形状变了两边一起红

## v1.50.0　(2026-08-25)
- 🔍 **查币卡片薄的真因找到了：整块数据消失却一个字不提**。
  查 AKE 有 8 行市值数据、查 TUT 一行都没有，看起来就像"这机器人信息很少"
- ✅ 现在缺了会写明**为什么**：市值块缺 = CoinGecko 没收录 / 它给的价和交易所
  差太多（同名币防误判）/ 刚被限频；买卖估算缺 = 四家现货里有的没这个币
- 📊 **新增基差**：合约价 vs 现货价。合约溢价说明有人愿意付钱做多，
  折价则反过来。两个价卡片上本来就有，白拿的一层情绪
- 📊 **新增流通率**：低于 30% 会提示大部分币还锁着、解锁就是持续抛压；
  FDV 是市值 2 倍以上也会点名
- 📊 **新增换手率**（24h量÷市值）：超过 100% 提示在被爆炒或刷量，
  低于 1% 提示几乎没人交易、进出都有滑点

## v1.49.3　(2026-08-25)
- 🐞 **部署完群里经常收不到更新内容**（不是每次，所以是时序问题）：
  启动播报挂在容器起来后 15 秒，那时连接常常还没稳。老写法**发失败也照样
  标记成"已播报"**，于是那一次全挂之后永远不再重试
- ✅ 现在一条都没发出去就**不标记**，另外每 5 分钟补发一次（已播过是空操作）。
  最坏情况从"永远收不到"变成"晚几分钟"
- 🩺 `/datacheck` 加了「更新播报」一节：当前版本、播没播过、订阅会话几个、
  实际目标几个。以前这条链出问题**完全看不见**，只能猜

## v1.49.2　(2026-08-25)
- 🐞 **置顶自动刷新每 10 分钟抛一次 NameError**：`handlers/howto.py` 里用了
  `log` 却没定义它。上一版发出去时测试全绿——因为那几条测试只做源码字符串检查，
  **从来没真正执行过那个函数**
- ✅ 补了真执行的测试：置顶的每条路径（首次发、编辑、钉不上、自动刷新、
  版本没变不动）都跑一遍
- 🛡 加一条全库护栏：任何模块用了 `log.` 就必须定义 `log`，
  防的是**整类**这种"从没被执行过所以没人发现"的错

## v1.49.1　(2026-08-25)
- 📌 **置顶消息写全了**：按 14 个分区列出 68 个命令，每个大类都写明
  对应的菜单入口在哪（以前只有 23 个命令、没写按钮位置）
- 🐞 **置顶失败的提示指错了地方**：说"需要置顶消息权限"，
  照着去「用户权限」里勾是**没用的**——那是群成员的默认权限。
  机器人置顶必须是**管理员**。提示现在把正确路径和错误路径都写出来
- 🩹 **钉不上也会把内容发出来**：以前发和钉绑在一起，钉失败就整件事算失败，
  屏幕上只有一句「❌ 置顶失败」，其实消息根本没发。现在内容先发，
  钉不上就告诉你可以长按手动置顶（之后照样自动更新，编辑不需要管理员）

## v1.49.0　(2026-08-25)
- 🐞 **链上监控建不了**：`/watchpct 0x… 5` 回「没查到 0X… 的价格」。
  命令解析先把地址大写成 `0X…`，下游就不认它是地址了。已修
- 🐞 **部署成功但群里没有更新内容**：ADMIN_CHAT_ID 里填的是群 id 时，
  更新播报会把整个群当成"管理员已看过"排除掉。现在只排除个人 id
- 🖼 **三张图改成 4小时 / 日线 / 周线**：相册第一张会被放大，
  把最需要看清细节的 4h 放前面。研判仍按日线算
- 🔒 **只让群成员私聊**：`/access on` 之后，**在群里的人私聊直接能用、
  退群自动失效**，不用一个个 `/allow`。`/access member off` 可关掉
- 📌 **置顶消息机器人自己维护**：群里发一次 `/pinhowto`，
  以后每次版本更新它**自动把置顶刷成最新**，末尾带上这一版新增了什么

## v1.48.1　(2026-08-25)
- 🐞 **更新播报一直断在半句话上**：CHANGELOG 里折行写的条目，解析时
  只收了第一行，后面被静默丢掉。已修，顺便把播报压成一句一条
- 📖 **播报末尾加上「怎么用：发 /howto」**：以前只说改了什么，
  看完还是不知道该怎么用
- 🔢 **查币卡片每个数字都配一句人话**：RSI 说清是超买还是中性；
  持仓量配着涨跌方向读（价涨仓增=新资金做多，价涨仓减=轧空要小心）；
  恐惧贪婪说清是全市场情绪、该怎么用
- ⚖️ **多空比改成和 `/lsr` 一样的说法**：`1.06　多头是空头的 1.1 倍（两边接近）`，
  并写明口径是**账户数**不是持仓金额。小于 1 的比值会翻成倍数
  （0.43 → 空头是多头的 2.3 倍），光看小数点人脑读不出量级

## v1.48.0　(2026-08-25)
- 💧 **LP 撤出告警（新）**：你用 `/watchpct 合约地址 5` 盯着的链上币，
  池子被抽水会自动推给你，不用另外订阅
- 💧 判据扣掉了币价因素：池子市值本来就跟着币价走，砸盘时流动性自然缩水，
  只有"少得比价格能解释的还多"才报，否则每次下跌都会误报跑路
- 📋 **下单前检查（新）**：实盘确认卡、虚拟盘开仓第 5 步，现在会告诉你
  这一单在不在资金费的付费那边、以及是不是和大多数散户站同一边
- ❤️ **告警自检（新）**：10 条告警链挂了心跳，哪条坏了会私聊管理员。
  以前告警任务挂掉的表现是「什么都没发生」，没人能发现
- 📡 数据源探测补上币安和 Bybit（以前只探 CoinGecko 和 OKX，而币安是主源）
- 🔬 `/oc` 链上卡片补「每笔均额」：几美元一笔多半是机器人刷量，
  几万一笔说明就那么几个人在定价

## v1.47.2　(2026-08-25)
- 🐞 **只有合约没有现货的币，一张图都发不出来，而且一声不吭**（他发 `ake` 时
  「啥也没有」）。取 K 线只查了币安现货和 OKX 现货——实测 AKE 币安现货 400、
  OKX 现货 51001，而**币安合约 200 有数据**。数据明明就在（信息卡上那行
  「合约 $0.009144 费率 Binance」就是它），只是没去那儿取。
  现在按 币安现货 → 币安合约 → OKX现货 → OKX合约 依次兜底
- 🗣 **画不出图时必须说一句**，不能只剩一张信息卡让人猜。
  现在会回「四个接口都没有它的 K 线，上面的行情数据仍然有效」
- 🏷 **K 线来源写在卡片上**：同一个币的现货和合约 K 线不是一条，
  只有合约的币画出来的是合约图，不说清会被当成现货读

## v1.47.1　(2026-08-25)
- 👀 **研判卡新增「反转扫描」一行，查过没命中也照印**：
  `反转扫描：顶背离✗ 底背离✗ 动能衰竭✗ 动能背离✗ 长影拒绝✗ 4h回头✗`。
  上一版只在命中时才显示，于是「这次没有背离」和「这机器人根本不看背离」
  在屏幕上一模一样——他看着卡片问「底背离顶背离在哪」，问得对。
  日线不足 40 根时会写明「这轮没查」，和「查了没有」分开
- 🗣 **「观望信号·弱」改成「观望」**：观望还分强弱是句废话，强度档只给买/卖。
  而且 ADX 很强、DI 又明确偏一边却给观望时，现在会说明为什么
  （「趋势强度够，但均线/动能没同向共振，等它们跟上再谈方向」）——
  不解释的话看起来像判据坏了
- 📏 **POC 单独一行并标口径**：它用全量 120 根算，而支撑阻力用近 30 根。
  并排挤在一行会出现「POC 比支撑还低」这种看着像算错的画面。
  现在带上距现价百分比，`$0.0115（-76%，全量120根）`，远近一眼看得出

## v1.47.0　(2026-08-25)
- 🐞 **修一个会让人追在高点的判据**：发币名时那张研判卡里，ADX 一直**只印在
  屏幕上、一分不参与判定**。而 ADX 只测趋势有多强、不带方向——一段猛跌里的
  逆势反抽同样能把 ADX 顶到 80 以上，反抽那几根上 MA/MACD/RSI 又全部翻多，
  于是结论是「买入·强，趋势较强可顺势」。**那恰恰是最容易被套的位置。**
  现在补上 +DI/-DI 方向校验：方向对不上就直接降到最弱并写明
  「ADX 高但 DI 方向相反，多半是逆势反抽，别当趋势追」
- 🧭 强度那行不再出现「可顺势」这种带方向的措辞。ADX 只说强弱，顺哪边由 DI 说了算；
  原来的写法碰上逆势反抽会一边写「可顺势」一边警告「别当趋势追」，自己跟自己打架
- 🔮 **研判新增三层判定**：ADX<20 的震荡市降级（顺势指标在横盘里会被反复打脸）、
  RSI 顶背离/底背离（只认超买超卖区，中间区的背离绝大多数是噪音）、
  早期反转预警（动能衰竭 / 动能背离 / 长影拒绝 / 4h RSI 回头，
  **结构型证据≥1 且共振≥2 分**才报——只靠动能会把强趋势里的普通回调也当反转）
- 📍 关键位那行补上 **POC 量能中枢**（成交量最集中的价位，磁吸位）；
  贴得很近时会提示「此处易反复」
- 💰 四所买卖聚合接进结论：喊多但近一日资金净流出会降级并说明「尚未获资金面配合」。
  这份数据**只取一次给两条消息共用**，不再各拉一遍四家交易所
- 🖼 **发币名现在给三张图一条相册**：周线 / 日线 / 4小时。单看日线的「多头排列」
  分不清它是周线趋势里的顺势、还是周线跌势里的一次反抽，而这两种情况该做的事完全相反。
  研判仍固定按日线算（MA3/13/23 的阈值是按日线定的语义）；某个周期取不到就少发一张，
  出任何岔子都退回原来的单张日线图
- ✅ `handlers/detail.py` 原来 545 行零测试——它是**发一个币名就会走到**的路径，
  用得比任何命令都频繁。补了 43 条，把上面每一条判据和降级规则都钉死

## v1.46.1　(2026-08-25)
- 🔔 **三个告警入口从第三层提到第二层**：`/menu` → 🔔 提醒与订阅 里直接就是
  【⚡急涨急跌】【📊合约异动】【🚨极端拉升】，每个都带 ✅已订阅／⬜未订阅。
  原来分别埋在「价格/条件提醒」和「定期订阅推送」里面，从菜单点下去要**三下**；
  原来的位置一个没删，那两个面板里照样点得到
- 🩹 **急涨急跌的覆盖范围在屏幕上写错了整整一版**：上一版币池已经换成
  币安 + Bybit 取并集（约 600 个），但订阅回执、告警抬头、滚动榜标题还写着
  「Bybit」「~500个」——少报一百多个币，还把币安独占的那批（PROM/PUMP/FET）
  说成不在监控里。取数改了文案没跟上，是这类不一致最容易漏的一种
- 📖 使用指南补上**告警订阅整章**：四类告警分别怎么订、怎么调阈值、怎么取消、
  口径是什么、自检怎么用。`docs/guide.md` 建好之后一次没更新过，
  而 `/pump3`、`/relay` 和 v1.43~v1.46 全部都是在它之后上的
- ✅ **网格 `/gridstart` 补上 43 条测试**：它会真下单，却一直是零测试——
  而 pytest 是发版门槛，零测试等于这条真钱路径上没有门槛。锁住了参数校验
  （不合理的参数一张单都不许发出去）、管理员门禁、每格初始方向、成交翻转记账、
  越界告警只报一次、以及**闸门继承**：`/killswitch` 关着时网格不能把单铺出去
- 🛡 顺带补三条护栏：指南里出现的命令必须真的存在（照着文档抄一条不存在的命令，
  机器人一声不吭，比文档缺一节更难查）、覆盖范围的说法必须和取数一致、
  告警入口不许再沉回第三层

## v1.46.0　(2026-08-25)
- ⚡ **急涨急跌扫描改成币安 + Bybit 取并集**，在场币从 Bybit 那一家的
  五百多个涨到 **609 个**。MNT / AGI / CASHCAT 和 PROM / PUMP / FET 现在都盯得到
- 为什么不是二选一：实测**币安永续 701 个、Bybit 732 个、两家都有 619 个**；
  只有币安且成交额≥500万的 **19** 个，只有 Bybit 的 **6** 个——
  **无论选哪一家，都固定看漏另一家独占的那批**
- 同一个币两家都有时留**成交额大**的那家（流动性更好、报价更有代表性）
- **一家挂了不影响另一家**：急涨急跌是每 60 秒的实时告警，
  不能因为一家抽风就整条哑掉。两家都出错才算失败
- 修一个写测试时当场抓到的：原来按"结果为空"判取数失败，
  那会把「取到了，但没有一个币过成交额门槛」也当成故障报出去。
  **「没有合格的币」和「取不到数据」是两回事**，混在一起日志里的错误就不能信了

## v1.45.1　(2026-08-25)
- 💣 **清算地图补上 Bybit**。之前只认币安，而告警是全交易所的——
  Bybit/OKX 独有的币（比如 AGI）自动配图那步会安静跳过，只剩按钮，
  点了也画不出来。现在币安没有就自动改用 Bybit
- Bybit 的持仓量是**币的个数**（币安直接给金额），要乘当时价格换成名义金额，
  否则两条路口径不一致、图的量级差几个数量级
- Bybit 的数据是新→旧、币安是旧→新，翻过来才对：不翻的话持仓量增减的正负
  全反，"加仓"被当成"减仓"，整张图直接空掉
- 修：点【💣 AGI】回一句「画不出来：'price'」。币安对不存在的币回
  400 + code -1121，而代码直接取了 `["price"]`，把一个光秃秃的
  KeyError 甩给了用户。**报错要说人话**——现在会讲清是哪两家都没有、为什么

## v1.45.0　(2026-08-25)
- 📊 **放量异动重写**。他问「怎么一天到晚都是这两个币放量」——查下来是
  **判据从根上就错了**，不是阈值没调好
- 老口径拿「这一轮的 24h 成交额 ÷ 上一轮的 24h 成交额」当倍数。
  **实测证伪**：90 秒内 149 个成交额≥200万的币，这个比值最大 **1.008**、
  中位数 1.000，没有一个到 1.5 倍——24h 累计值在几分钟里根本不会动。
  所以真放量永远测不到，能触发的只有基线是脏数据的时候
- 加上**完全没有冷却**，同一件事每 5 分钟重报一次，就成了 UTK/LRC 天天刷屏
- 新口径：**这段时间的净增量 ÷ 它自己的平均速率**。
  「近 5 分钟成交了多少」÷「平时 5 分钟该成交多少」，零额外请求。
  24h 一千万的币 5 分钟成交 20 万 = 平时的 5.8 倍，这才是放量
- 三道护栏：绝对量至少 30 万（小池子按比例能炸出任何倍数）、
  同币 4 小时冷却、两次快照间隔要在 2 分钟~1 小时之间
  （重启后基线过期，那一轮只重建不告警）
- 告警文案把口径写在脸上：「近 5 分钟成交 $XX，是平时同时长的 5.8 倍」，
  而不是干巴巴一句「量增 28 倍」
- 这个模块原来零测试（属于那 7 个欠账之一），补了 12 条

## v1.44.2　(2026-08-25)
- 🧪 **`/alertnow` 现在和真告警长得一模一样**：带清算地图、带【💣 币名】按钮
- 这条路径的用途就是自查「告警到底工不工作」——真告警带图带按钮，
  而补推是光秃秃的文字的话，等于那一半根本验不到。
  想测试配图功能，发一次 `/alertnow` 就行，不用干等行情
- 当前没有 ≥门槛 的异动时它会明说「通道正常，但当前没有」，
  并提示去 `/contract` 把最低档调低

## v1.44.1　(2026-08-25)
- 💣 告警配的清算地图，**文案按方向分**了。涨和跌该盯的不是同一侧：
  砸下来要看**下方还有多少多单**等着被连环打掉（那是继续往下的燃料），
  拉上去要看**上方还有多少空单**会被逼。两边一个文案，等于把最该说的那句省了
- 图注里直接给**最密的那一档**：价位区间、约多少 U、距现价百分之几。
  不用点开图也知道下一个坎在哪
- （大涨大跌一直都带图——判据是幅度绝对值，不分涨跌。v1.43.0 就加了）

## v1.44.0　(2026-08-25)
- 📡 搬运的关键词过滤**可以按频道分开设**了：
  `/relay only @某频道 币 BTC 加密` 只对这一个频道生效
- 为什么必须分开：搬推特（马斯克/特朗普/CZ）的频道里九成不是币圈内容，
  得用「只要含币圈词」筛；而律动那种频道本来就全是币圈内容，
  同一把尺子会把好东西误杀。一套全局规则表达不了「这个要筛、那个不筛」
- 没给某个频道单独设，就沿用全局那套；面板上把每个频道自己的规则列出来
  （设了看不见等于没设）
- 顺带记一下探针结论：**免费拿推特这条路已经堵死**——X 官方 API 401、
  官方 syndication 返回空、Nitter 各实例死的死要验证的要验证、
  xcancel 要人工白名单、RSSHub 公共实例 twitter 路由 404。
  所以推特内容走「订阅现成的搬运频道 + /relay」，别去抓

## v1.43.1　(2026-08-25)
- 修：目标已经是当前群时再点【📍 转发到当前会话】**看起来毫无反应**。
  实际动作执行了，但面板内容一个字没变，Telegram 不允许把消息编辑成
  完全一样的内容，于是屏幕上什么都不动——分不清"成功了"还是"坏了"
- 现在会**弹窗**告诉你「这里本来就是转发目标了，不用再点」。
  **凡是"操作成功但没有可见变化"的按钮，都得弹一下**，否则只会让人一直点
- 搬运面板补一句「**只转新发的帖，不补历史**」——刚配好时
  「已转 0 条」是正常的，不说清楚会被当成功能坏了

## v1.43.0　(2026-08-25)
- 💣 **合约异动告警现在带清算地图**。异动之后最该问的一句就是
  「上下还堆着多少待强平的仓位」，那正是这张图回答的
- **一条告警只自动配一张图**：挑幅度最大的那个币（且幅度 ≥25% 才值得画）。
  一轮可能同时报十个币，每个都画就是又慢又刷屏
- 其余的币在告警消息底下给【💣 币名】按钮，最多 4 个，点一下就出图，
  币名直接带进去不用再选
- 配图失败**一律安静跳过**：清算地图只走币安永续，而告警是全交易所的，
  取不到是常态不是异常。告警本身已经送到了，不能因为配图画不出来
  就在群里再刷一条"失败"

## v1.42.4　(2026-08-25)
- 搬运自检现在**知道你是在哪儿发的**。人已经站在群里了，却还提示
  "去你要收消息的那个群里发 /relay here"——读起来像还要再去别的地方。
  现在在群里跑就直接说：**你现在就在群里，直接发 `/relay here` 就设好了**
- 顺带点破一句「`/relay check` 只自检、不会改目标」——
  check 和 here 长得像，很容易把前者当后者用
- 目标群就是当前这个群时，自检会写「（就是当前这个群）」，
  省得你怀疑到底设上没有

## v1.42.3　(2026-08-25)
- 修：搬运自检把**私聊目标**误诊成"群进不去"。在和机器人的私聊里发
  `/relay here` 会把目标设成你自己（一个用户 id），这时候提示却说
  "用主号把搬运号拉进那个群"——**根本没有群，指错方向比不提示更浪费时间**
- Telegram 的 id 有符号约定：**正数是用户，负数才是群/频道**。现在按这个分开判：
  正数 → 直接告诉你"这是私聊不是群，去群里再发一次 `/relay here`"，
  并说明搬运号也发不了私聊给你（两个号之间没有过对话）；
  负数进不去 → 才是"把搬运号拉进群"
- 面板上也提前标出来，不用等你跑自检才发现

## v1.42.2　(2026-08-25)
- 修：搬运面板上的频道名**多出一个反斜杠**。频道名带下划线是常态
  （他要接的第一个 blockbeats\_chart 就是），而旧版 Markdown 在反引号里
  **不处理转义**——转义之后屏幕上就真的多出那个反斜杠，照着复制去
  `/relay add` 就是错的。比难看严重的是：给出的东西照抄必错

## v1.42.1　(2026-08-25)
- 📡 搬运的 session **可以是小号的**，不必是你本人的账号——它只是"某个账号的
  凭证"，代码不关心是谁。用小号反而更好：主号不暴露在自动化风险里。
  前提是那个号**自己订阅了源频道、而且在目标群里**
- ⚠️ 但别现注册新号：**全新账号被限制的概率反而更高**——刚注册、没有任何
  正常聊天记录、上来就只做自动转发，是标准的 spam 特征。用有使用历史的老号
- 🩺 **加了自检**（`/relay check`，面板上也有按钮）。因为换了小号之后多出一个
  失效模式：**配置的是你，干活的是那个号**——你在面板上把目标群设好了，
  不代表那个号进得去。转发失败只写日志，你这边看到的就是"没反应"
- 自检逐条实测并说人话：干活的是哪个号、目标群进没进去（没进就告诉你
  "用主号把它拉进去"）、每个源频道订没订、以及**该频道是不是开了「禁止转发
  内容」**——开了的话订阅了也搬不出来，不检这条的表现就是"没报错但不转"

## v1.42.0　(2026-08-24)
- 📡 **频道搬运** `/relay`：把你订阅的频道的新帖自动转到自己群里
- 为什么要用你的账号：**Bot API 读不到机器人自己不是管理员的频道**，
  别人的频道不可能给你加管理员——这是接口的硬限制，不是配置问题。
  所以走 MTProto，用你的账号登录一次（`tools_tg_login.py` 换一串 session）
- ⚠️ **这是拿个人账号做自动化，Telegram 可能限制账号**。所以：
  没配 TG\_SESSION 就**完全不启动**（机器人和平时一模一样）、默认**关闭**、
  仅管理员能配、风险写在面板上而不是只写在文档里
- 用**原生转发**不是复制内容：自带「转发自 XXX」的头，图文格式一个不丢，
  出处天然正确——搬别人的东西，出处不能弄丢
- 关键词过滤：`/relay only 解锁 上币` 只转含这些词的；
  `/relay skip 广告` 不转含这些词的。**排除优先于包含**——
  「我要解锁消息但广告一律不要」这个意图只有这样才表达得出来
- 每个频道每小时最多转 20 条，某个频道抽风连发不会把群淹了
- 私密频道用数字 id，公开频道 @名字、t.me 链接、纯名字都认

## v1.41.1　(2026-08-24)
- 💣 **查完币价那张卡上直接给【清算地图】按钮**，币名自动带进去，不用再打命令
  也不用再选一次币。分析完最该问的下一件事就是"止损放哪儿"，
  而那张图直接回答它——少这个按钮，分析和下单之间就还隔着一次手工搬运
- 想直接查某个合约仍然是 `/liqmap TRUMP`（`/lmap SOL` 短写法），两条路都通

## v1.41.0　(2026-08-24)
- 🚨 **极端拉升告警** `/pump3`：15 分钟内放量暴拉 **且** 3 日累计已经涨了一大截，
  两个条件**同时**满足才推。菜单 → 🔔 提醒与订阅 → 🔔 价格/条件提醒 → 🚨 极端拉升
- **只做订阅不做命令，是量出来的**：探针实测过去 24 小时、112 个成交额≥500万的
  永续里，符合「15m≥40% 且 3日≥50%」的是 **0 个**。这个组合本来就稀有——
  做成命令等于点开永远空白，做成订阅才是一个月响几次、每次都值得看
- 门槛全按钮可调：15m 20/30/40/50%｜3日 不限/30/50/80%｜量比 不看/1.5/2/3×。
  也认 `/pump3 40 50 2` 这种直接打的写法
- 每条告警带**位置标签**，不是只甩百分比。同一根暴涨 K 线，配上不同的 3 日涨幅
  意思完全相反——实测那天唯一 15m 涨过 40% 的是 VELVET(+54.9%)，
  而它 3 日是 **−79.2%**，那是砸完之后的超跌反弹，不是他要抓的形态。
  标签分四类：超跌反弹 / 横盘启动 / 顺势加速 / 末端逼空
- **先闸便宜的再拉贵的**：15m 涨幅复用急涨急跌那个 60 秒任务的行情，白拿；
  3 日涨幅和量比要各拉一次 K 线，**只对过了 15m 闸的币拉**（实测一天 0~1 个），
  等于几乎不产生额外请求
- 量比只用**已收盘**的 K 线：取最近 3 根收盘的 5m（正好 15 分钟）比前 36 根的均值。
  用未收盘那根会系统性偏低
- 加了【🔍 现在有没有】自检：这种告警一个月响几次，**"没响"和"坏了"看起来
  一模一样**。点一下现扫一遍，逐个列出过了哪几关、卡在哪一关
- 同一个币 6 小时内不重复报
- 修一个会静默失效的地方：那个 60 秒任务原来只看急涨急跌的订阅，
  **只订了极端拉升的人它一次都不会跑**（和 v1.35.0 现货止盈损同一个坑）

## v1.40.1　(2026-08-24)
- 📖 **`/howto`**：群里发一下就把使用指南贴出来，新进群的人不用翻聊天记录。
  也在 /menu → ❓ 帮助 → 📖 怎么用 里
- 指南本身进了仓库（`docs/guide.md`），跟代码一起版本管理，
  改功能时顺手改它，链接不变：
  github.com/logan775800/crypto-bot/blob/main/docs/guide.md
- 消息底下带按钮直接跳：3日涨跌榜 / 多空比 / 清算地图 / 功能菜单
- **群里不放「虚拟交易台」按钮**：按钮回调是就地编辑原消息，
  在群里点一下等于把自己的持仓贴给全群看。私聊才有那个入口
- 和 /help 的分工：/help 是全部命令的清单（给已经会用的人查），
  这条是怎么上手（给不知道从哪点起的人）

## v1.40.0　(2026-08-24)
- 💣 **清算地图** `/liqmap TRUMP`：各个价位上估计堆了多少待强平仓位，画成图发给你。
  菜单 → 📈 分析与图表 → 💣 清算地图（两下，点币种就出）
- ⚠️ **这是模型估算，不是交易所数据。**没有任何交易所公布「某个价位挂着多少
  待爆仓的仓位」——那是每个账户的私有信息，CoinGlass 那张图也是推算的。
  币安的历史强平单接口已经 404，CoinGlass 的 heatmap 不在免费档，
  能白拿的只有持仓量历史和 K 线。所以「估算」两个字写在标题、图注、
  图片左下角三个地方——图会被单独转发出去，脱离文字也不能让人误读
- 怎么推算的：每根 K 线期间**新增**的持仓量当成建在那根的典型价上（减仓不算，
  那是平仓不产生新爆仓位）；永续里每份持仓同时是一多一空，所以两侧都留下位置：
  多头爆仓 = 建仓价 × (1 − 1/杠杆)，空头 = × (1 + 1/杠杆)；
  按 5x/10x/25x/50x 分配权重落进价格桶，现价已经越过的一侧抹掉
- **假设全部印在 ℹ️ 里**：杠杆分布是拍的（交易所不公布谁开了几倍）、
  建仓价用 K 线典型价、没算维持保证金率（要 API key）所以位置比真实爆仓价略远
- 图下面直接给**上下各 3 个最密集的价位区间**和距现价百分比——
  图是给人看的，这个是给人用的
- 窗口 1日 / 7日 / 30日 一键切，还有【🔄 重算】【🪙 换币】
- 怎么用：密集区是磁吸位，价格容易被推过去扫一轮再走；
  **止损别正好压在柱子上**，那正是最容易被插针带走的位置

## v1.39.0　(2026-08-23)
- ⚖️ **多空比极值榜** `/lsr`：全市场最被看多 / 最被看空各 3 个。
  菜单 → 📊 行情 → ⚖️ 多空比极值榜（两下）
- 每行直接把数字翻译成人话：`0.46　多头31%　空头是多头的 2.2 倍`。
  小于 1 的比值人脑读不出量级，这正是群里问的那件事
- 带 BTC 作参照——极值好看，但没有参照系读不出"这算不算极端"
- **不合并三家，只换所。** 探针实测同一时刻 ETH：币安 2.69 / OKX 1.33 /
  Bybit 1.86。各家统计的是自己那批用户，合成一张榜排出来的是
  「哪家用户最偏多」，不是「哪个币最被看多」
- **不接 OKX**：它的接口按币种收录、只有主流币（TUT / AGI 这些直接报错），
  而极值恰恰常出在冷门币上。`/ratio BTC` 单币查询仍然走 OKX，没动
- 成交额门槛 2000 万，比涨跌榜高——多空比是**账户数**统计，
  池子太小时一个人开仓就能把比值打飞，那种极值是噪音
- 真机第一跑抓到两个问题，都修了：
  ① 「最被看多」榜首是 **SOXL**（3 倍做多半导体 ETF），第二档还有 CL（原油）。
     我复用了涨跌榜的过滤却漏了品类那条——同一个坑第三次，这次剔了 173 个
  ② Bybit 的「最被看空」里躺着 `ZRO 1.01`，那根本不是看空。
     原来是"取头 3 + 取尾 3"，改成**以 1 为界切开**；
     真正偏空的不足 3 个时就老实写 Top2

## v1.38.1　(2026-08-23)
- 📅 涨跌榜的入口从**三层提到两层**：`/menu` → 📊 行情 → 📅 3日涨跌榜。
  原来埋在「💰 查币价/涨跌榜」里面，要点三下才够得着——埋这么深等于没有入口
- 榜单是进「行情」最常想看的东西，不该比查单个币还深

## v1.38.0　(2026-08-23)
- 🔥 **涨跌榜默认只看热榜**（成交额前 50 名的币）。群里的原话是「最好只看热榜的」——
  全量榜一屏全是没听过的代号（AGI、USELESS 这种），涨得再多也不知道该不该碰
- 【🔥 热榜前50】【全部币】一键切。**这是纯显示过滤，用的还是同一批数据**，
  切换是一次重排不是一次重扫，点了就出
- 50 是实测出来的下限：卡到成交额前 30 名（门槛 1 亿）时**跌幅榜整个是空的**，
  一张没有跌幅榜的涨跌榜没有意义；前 50（门槛 4800 万）才有 5 个
- 卡片上写明「成交额前 50 名里排的（共 213 个币，点【全部】看全量）」——
  不写的话，热榜看起来就像"全市场就这几个"
- `/rank 3 全部` 也能直接要全量

## v1.37.0　(2026-08-23)
- 📅 涨跌榜现在**币安 / Bybit / OKX 三家 × 永续 + 现货全都扫**，
  不再只有两家的永续。3400 个交易对，去重后 213 个币里排
- **按钮全在，而且看得见了**：上一版卡片一屏 27 行，Telegram 的按钮永远在
  消息末尾，结果按钮被挤出屏幕——等于没做。现在每组显示 8 个、去掉分隔线、
  细账收进【ℹ️ 口径】，卡片 24 行，末尾还写明「👇 下面可换窗口/交易所/现货」
- 四排按钮：3日/7日/14日｜三家/Bybit/币安/OKX｜永续+现货/只永续/只现货｜
  ℹ️口径 🔄重扫 🏠。换窗口、换所、换市场都读缓存，不重扫
- **先去重再拉日线**：同一个币在六个盘子里出现四五次，
  先用 6 个便宜请求把「有哪些币+成交额」拿全、按底层资产去重、
  只留成交额最大的那个盘子，再只对去重后的拉日线。
  反过来做等于把四分之三的请求花在重复的币上
- 修一个**静默丢数据**：单独扫 OKX 时 94 个候选只回来 40 个，
  **54 个被一句 except 吞掉**，榜还照排、看起来一模一样完整。
  现在按交易所分开限流 + 失败退避重试，实测 94→88；
  仍然没取到的会在卡片上写明「N 个币的日线没取到」
- 修：`USDG` `RLUSD` 这类新稳定币以 -0.1% 占了跌幅榜三个位置。
  改成**按价格判**（整段贴着 1 美元的就是稳定币），不再靠维护名单
- 修：`SPCXB`(SpaceX) 这类代币化股票混进现货榜。币安现货接口里
  **没有任何品类字段**（实测 SPCXB 和 BTCUSDT 的 permissionSets 一模一样），
  现有那套品类判据认不出，只能靠名单挡——ℹ️ 卡片上明说了这类可能有漏网
- 币安日线是**旧→新**，和 Bybit/OKX 正好相反，写了测试盯着：
  不反转算出来的是另一段时间，而且符号经常还是对的，肉眼看不出错
- 等待提示改成「十几到几十秒」——实测三家全扫要 30 多秒，写少了会让人以为坏了

## v1.36.0　(2026-08-23)
- 📅 **多日涨跌榜**：`/rank 3`、`/rank 7`（`/top 3` 也一样），
  菜单 → 行情 → 查币价/涨跌榜 里有【3日】【7日】按钮
- 以前所有的榜都是 24h，问"这三天谁涨得最多"答不上来。
  **和 `/upstreak` 不是一回事**：那个要连续几天同向，
  一个币 3 天累计涨 30% 但中间回调过一天，在连涨榜里根本不存在
- **一次扫描答所有窗口**：日线一次拉够，3日/7日/14日按钮换着看走缓存不重扫
  （重扫要十几秒，而那十几秒里你只是想换个窗口）
- 口径写在卡片上：**现价 vs N 天前的日线收盘**。终点用现价而不是昨收——
  否则今天的行情完全不体现，读出来是一笔隔夜的旧账
- 真机第一次跑就抓到三个问题，都修了：
  ① **1000PEPE 和 PEPE 并排出现在第 9、10 名**——同一个币，只是合约面值不同。
     现在按底层资产合并、留成交额大的那家。但只合并纯面值前缀，
     不按"名字像"合（PUMP 和 PUMPFUN 必须分开，合错就是把两个币算到一起）
  ② **SNDK 这类代币化美股混进跌幅榜**——OKX 的合约接口没有品类字段，
     用已有的判据剔掉了，剔了几个会报出来
  ③ 涨幅榜和跌幅榜原来是"前 15 + 后 15"，币少的时候
     **-30% 的币会列在涨幅榜第三名**。改成按正负切开
- 全市场一边倒时，**空的那组也印出来**并给出"跌得最少/涨得最少的是谁"——
  整段消失读起来像没扫那一边，而"这 3 天一个上涨的都没有"本身就是信息
- `/top`（24h 那张）也补了口径行：它走 CoinGecko 全市场现货，
  和这张交易所永续榜不是同一个盘子

## v1.35.0　(2026-08-23)
- 🎯 **现货也能挂止盈止损了**。以前只有永续有（`/vtpsl`），现货买了就只能自己盯着。
  当时的理由是"现货不会爆仓"——但那只说明不会被强制带走，不代表不该有计划
- 交易台的现货那行加了【🎯止盈损】，和合约那行**同一个形状**
  （平/全平/改止损 ↔ 卖50%/全卖/止盈损）。少一个入口，练出来的手感就断一截
- 命令还是 `/vtpsl BTC sl=60000`：有合约仓就设合约仓，没有就落到现货持币上；
  两边都有时加「现货」指名道姓，回执里也会写清这次动的是哪一个
- **方向判据用现价，不是成本均价**（和永续那边刻意不同）：买在 5 万、涨到 7.5 万
  之后把止损抬到 6 万正是该做的动作，拿成本价卡会把最该设的那个止损拒掉。
  真正会出事的只有"挂上去 60 秒内就触发"，那是个现价问题
- **触发时会先撤掉这个币挂着的限价卖单再全卖**：锁在卖单里的币卖不掉，
  不撤的话止损只卖得动一部分，剩下的继续躺在一张远得多的单子里。
  同币的限价**买**单不动——那是"想在更低的价接回来"，是另一笔决定
- 修一个静默失效：后台那个 60 秒任务原来只按**合约持仓**收集要查价的币，
  所以**只玩现货的人它一次都不会跑到**，止盈损永远不触发。
  不报错、日志干净，只是某件事永远不发生
- 平仓记录分得清「我自己卖的」和「被止损带走的」（exit_kind 写「现货止损/止盈」），
  否则复盘里看不出止损到底救了他还是害了他

## v1.34.1　(2026-08-20)
- 扫描**能扫出更多币了**：实测做多从 9~12 个涨到 15 个，做空组也不再总是空的
- 原因是三个上限叠在一起，最要命的是**粗筛门槛**：成交额≥2000万时，
  全 Bybit 728 个永续对里只剩 38 个候选——门槛本身成了瓶颈。
  而扫描算滑点用的参考名义才 5000U，1000万日成交额对这个量级绰绰有余。
  **降门槛不等于降标准**：真正把关的是逐个查真实订单簿的否决，那一层没动
- 候选池 40→60、细算 16→24、每组显示 12→20；截断时会写明「还有 N 个没显示」
- 修一个连带 bug：流动性评分**把粗筛门槛当成了对数基准**，
  所以我一调门槛，所有币的流动性分就静默变高、连带把否决线也挪了。
  现在评分基准固定，和门槛彻底分开
- 剔除理由去重时先抹掉数字：「波动过大(13.9%)」和「波动过大(8.7%)」是同一类，
  不抹的话两条都列出来，读起来像两个不同的问题

## v1.34.0　(2026-08-20)
- 永续扫描改说「**做多 / 做空**」，不再说"买入/卖出"——永续能双向开仓，
  用现货的说法会让人以为只能做多。现货来源仍然说买入/卖出
- **做空 0 个时也把这一行印出来**。整段消失读起来像"没扫做空"，
  而"当前一个空头共振都没有"本身就是有用的信息（市场一边倒）
- **覆盖从 16 个涨到 40 个，耗时反而从 15~30 秒降到 10 秒左右**：
  改成两段式——先用只取 K 线的便宜通道给全池打信号标签，
  再只对**真有信号**的那十几个跑盘口/拥挤度否决。
  以前按成交额取前 16 个做全套细算，排第 20 位但三周期共振的币根本轮不到被看一眼
- 结果下面加了【🏦 换所 / 换现货】：八个盘子（四家 × 现货/永续）点一下就重扫。
  这个能力一直有，但入口埋在 /source 全局设置里，等于没有
- 口径写在脸上：「打过标签 39 个，其中 16 个做了细算」+
  「只扫 Bybit永续——不含现货、不含其它交易所」

## v1.33.1　(2026-08-20)
- 修：上一版的信号扫描**几乎全是「中·多头共振」**，一个「强」都没有——三个 bug 叠在一起
- ① **放量永远不触发**：拿的是最后一根**还没收盘**的 K 线，量只累积了一部分。
  实测 4h 的比值 1.1~2.4、1h 0.28~1.37、15m 只有 0.13~0.67，越短的周期被压得越狠。
  改成用**已收盘那根**比
- ② **金叉被丢**：只看最短周期。真机上 XLM 的 4h 和 1h 双双金叉，
  因为 15m 没交叉就整个丢了——而大周期的金叉本来更有分量。现在看所有周期
- ③ **多头贴着压力位却什么都不说**：原来只挑顺方向的标签，逆方向的默默丢掉。
  现在会标「⚠️上方有压力」，但用**严得多**的距离阈值——
  上涨趋势里价格本来就靠近近期高点，同一个尺子会让 10 个信号 6 个都挂，
  不区分就是噪音
- 剔除理由现在说人话：「（流动性不足）」而不是「（❌ 不建议）」

## v1.33.0　(2026-08-20)
- 🔍 **扫描结果改成一行一个币**，买卖分开：
  `ENA  0.0976  强·多头共振+放量`。原来每个币铺 6 行，一屏只看得到一个半
- 新增**信号标签**：多头/空头共振、金叉/死叉、放量、贴支撑/贴压力。
  共振的定义是**多周期均线同向**——1h 多 4h 空的币，哪个方向进去都是在猜，
  这种不进名单
- **强/中分级**：命中 3 条以上算强，强的排前面（不看四维分数）
- 四维明细（趋势/流动性/拥挤/执行）没删，收进【📊 四维明细】按钮，
  点开看的是刚才那批结果，不用再等半分钟重扫
- **否决逻辑保留**：指标漂亮但盘口太薄/费率极端/顺势方向恰是拥挤方的币，
  不混进名单，但会报出剔除了几个、为什么
- 细算范围从 12 个提到 16 个，并在结果里写明「只看了成交额前 N 个，
  排在后面的币即使有信号也扫不到」——不说的话这张名单看起来像"全市场就这几个"

## v1.32.0　(2026-08-20)
- 🧹 **首页重排**：入口从 23 个降到 10 个，一屏能看完。
  靠的是**合并同类，不是把功能藏起来**——原来的入口一个不少，全都还点得到
- 四个交易所专区（OKX/币安/Bybit/Gate）合成一个「🏦 交易所专区」，进去选所。
  它们本来就是同一批功能的四份拷贝，占了首页 4 个位置
- 合并的还有：市场看板+行情查询 → 行情；订阅推送+价格预警 → 提醒与订阅；
  实用工具 → 交易工具；策略回测 → 分析与图表；我的持仓 → 风险中心
- 首页原来混着**三套分类逻辑**（按数据类型 / 按交易所 / 按场景），
  这才是"又杂又乱"的真正来源。现在统一按场景分，每个入口回答一个「我要干什么」
- 虚拟合约的首页入口直接进**交易台**（全按钮那个），不用再多点一层
- 仍然**没有「更多」折叠层**——那个方案 v1.8.0 试过、v1.10.1 已经撤回

## v1.31.1　(2026-08-20)
- 币安端点探测补两句该说的话：验出是**实盘 key** 时明确警告「设成 false 之后
  所有下单都是真钱」（Bybit 那边本来就有这句，币安漏了）
- **钱包余额为 0 会点名**：认得这把 key ≠ 下得了单。币安的现货和合约是两个钱包，
  合约钱包空着就得先在币安 App 里划转过去，否则下单只会被拒
- 切换交易所时直接把余额显示出来——切过去才发现钱不在这个钱包，太浪费时间

## v1.31.0　(2026-08-20)
- 🏦 **接入币安**（USDT 本位合约 + 现货）。`/venue binance` 切过去，
  `/trade` 交易台、`/rpos` `/rbal` `/rorders` 这一整套就都走币安了
- 做法是**换客户端不换逻辑**：交易那 900 多行风控（二次确认、平仓强制
  reduceOnly、杠杆护栏、爆仓预警、操作审计）两家共用一套。
  复制一份等于同一套风控维护两遍，分叉那天会在真钱路径上发现
- killswitch 和 LIVE_TRADING 硬开关**换所照样拦得住**——换个交易所就绕过去的话，
  那个开关等于没有
- 币安默认走**测试网**，认不出来的值一律当测试网（和 Bybit 同一条规矩，防手滑上实盘）
- 端点探测：`docker compose exec crypto-bot python binance_trade.py probe`，
  合约/现货 × 测试网/实盘 四种各试一次，直接告诉你这把 key 属于哪套
- 交易台上的环境标现在写**哪一家 + 是不是真钱**（如「币安🧪测试网」）。
  多了一家之后，只写环境的话「我这单下在哪儿」根本看不出来

## v1.30.0　(2026-08-18)
- 🚦 **一个慢操作不再把所有人堵住**。以前跑动量回测时，任何人发的任何消息都在
  排队等它跑完——表现出来就是「机器人卡死了」，发 /menu 石沉大海。
  现在并发处理，慢活在后台跑，其他功能照常用
- 这也是之前那批「回调应答过期」报错的同一个根因
- 重活加了防重复触发：同一个人的动量回测一次只能跑一个，
  再点会告诉你「上一次还在跑（已 42 秒）」，而不是默默排队
  （排队的表现和卡死一模一样）
- 进度提示改成说实话：原来写「约 30~60 秒」，实际要 1~3 分钟——
  低估等待时间会让人以为坏了。现在也写明「这期间其他功能照常能用」

## v1.29.1　(2026-08-18)
- 实盘开仓的**保证金也改成按钮**了：选完杠杆直接给档位（50/100/200/500/1000
  和 10%/25%/50% 仓位，按你真实可用余额过滤），只剩价格要打字。
  想自己填金额点【✍️ 自己输】
- 保证金填 0 时会说清它是干什么的：保证金 × 杠杆 = 仓位价值，所以不能是 0
- 🔴 **引导开仓的每一句提示都标出是哪个账户**（实盘/模拟交易/测试站）。
  这套引导和虚拟盘长得几乎一样，而实盘这边一确认就是真钱，之前那句
  「请发「保证金 价格」」一个字都没提
- 修：实盘开仓的引导状态是老写法，**永不过期、跨会话生效**——
  隔很久之后随手发的一句话可能被当成开仓参数。现在 5 分钟自动失效、
  只在点按钮的那个会话里生效

## v1.29.0　(2026-08-18)
- 🎮 **虚拟交易台**：虚拟盘全部改成按钮操作，不用记任何命令。
  `/vtrade` 或菜单 → 虚拟合约 → 打开交易台
- 开合约五步全是按钮：选币 → 方向 → 杠杆 → 保证金 → 市价/限价。
  和**实盘交易台走同一套路径**——模拟盘的意义是手感能迁移，
  如果两边点法不一样，那点手感也白练了
- 买现货：选币 → 选金额 → 市价/限价，一样全按钮
- 持仓行自带【平50%】【全平】【改止损】；现货行自带【卖50%】【全卖】；
  每张委托单自带【撤单】，还有【全部撤销】
- 只剩两处要打字，因为它们没有合理的档位可选：**限价单的价格**、
  **「其他币」的代号**（中文名的币也认，比如「牛来」）
- 做多把限价挂在现价之上会直接告诉你「这等于市价单」，而不是默默当挂单收下

## v1.28.0　(2026-08-18)
- 虚拟盘补齐成**完整的模拟交易所**：委托单 + 现货，和实盘一样的操作路径
- **限价委托**：`/vopen BTC long 1000 10 60000` 现在是真的挂在那儿等，
  到价才成交。以前是「假装以 60000 成交了」——那会养出"我总能在想要的价位进场"
  这个最贵的错觉，而挂多远、等多久、要不要追，恰恰是最该练的
- 挂单会**冻结资金**（不冻结账户余额就是假的），成交走 0.02% 挂单费率、
  比吃单便宜——这是挂单的真实优势，不体现出来就少了一个该练的决策维度
- **现货**：`/vbuy BTC 1000` 买、`/vsell BTC all` 卖、`/vspot` 看持币，
  都支持限价委托。现货**不会爆仓、不扣资金费**，这是和永续最本质的区别
- 现货算加权成本均价、按比例结转成本，卖出后进同一份历史统计
- `/vorders` 看委托单（带距现价百分比）、`/vcancel 3` 撤单、`/vcancel all` 全撤
- 菜单里加了「现货持币」「我的委托单」两个按钮

## v1.27.2　(2026-08-18)
- 交易台和余额卡上的环境标从两种改成三种：🔴实盘 / 🧪模拟交易 / 🧪测试站。
  两套模拟盘是**不同的两个账户**，都标成「模拟盘」的话，换了 key 却忘改
  BYBIT_TESTNET 时屏幕上看不出任何异样

## v1.27.1　(2026-08-18)
- 修：`LIVE_TRADING=off` 会把**模拟盘**的开仓也一起拦掉。这个开关的意思是
  「我不做实盘」，不是「我不做交易」——切到 Bybit 模拟交易想练手，
  结果被一个叫 LIVE_TRADING 的开关挡住，说不通也很难自己想明白
- 现在它只在实盘模式下生效；模拟盘（demo / testnet）下单照常。
  `/killswitch` 仍然对两边都生效——它是紧急停手，管的事不一样

## v1.27.0　(2026-08-18)
- 新增 🚪 **准入控制**：`/access on` 之后，白名单外的人用不了这个机器人
  （在这之前是完全敞开的——任何人搜到就能私聊它问 AI、跑全市场扫描，花的是你的额度）
- 群按会话授权（群里所有人都能用），私聊按人授权：`/allow <id>` `/deny <id>` `/allowed`
- 开启的那一刻会自动把你和当前会话放进名单，**不会把你自己锁在门外**；
  `/access` `/allowed` `/id` 这些开关类命令永远放行
- 有人被挡时会把他的名字和 id 私聊发给你，直接 `/allow <id>` 就放行，不用去问对方
- 被挡的人只提示一次（10 分钟内不再回），陌生人刷命令换不来一堆回复
- 按钮点击同样受管——只挡消息不挡按钮等于没挡
- 新增 `LIVE_TRADING=off` 硬开关：只想用虚拟盘就在 .env 里设它，
  实盘开仓彻底禁掉、`/killswitch` 也打不开（比软开关硬，不会因数据恢复而悄悄变回可下单）；
  查询和平仓不受影响

## v1.26.5　(2026-08-18)
- 🚨 修一个**把危险配置报成安全**的 bug：Bybit 对「不限制 IP」返回的是 `*`，
  而 /keycheck 看到这个非空值就判定「绑了 IP」，还打绿勾说「这是该有的配置」。
  实际上 `*` 等于任何机器都能拿这把 key 下单
- 现在 `*` 一律判为**未绑定**，并说明后果：泄露即可下单；而且不绑 IP 的 key
  Bybit 只给 90 天，到期会静默失效（那天起账户功能全变空，很难联想到原因）
- 到期时间旁边会注明「不绑 IP 的 key 只有 90 天」
- 多勾的权限（Spot/Options/Derivatives）会提示下次轮换时收窄——机器人只用永续合约。
  这条算建议不算风险，不会把「配置合格」那句绿勾顶掉

## v1.26.4　(2026-08-18)
- 修：密钥排查提示里的 BYBIT_TESTNET 显示成了 BYBITTESTNET（下划线被吃掉），
  整段还变成斜体。Telegram 会把任意两个下划线配成一对斜体标记——
  等于**排查提示本身给出了错误的命令**，照着抄必错
- 这类提示改成纯文本发送，并加了护栏测试盯着：带下划线变量名的消息不许走 Markdown

## v1.26.3　(2026-08-18)
- 修：`/keycheck` 失败时只甩交易所原话 + 一张排查表，**连自己在打哪个端点都没写**，
  而排查表第一条恰恰是「端点对不上」——看的人没法对号入座
- 现在失败会先用查余额再验一次同一把 key，两种情况分开说：
  · 余额查得通 → 密钥和端点都没问题，只是这把 key 调不了「查询API信息」这个接口。
    实盘下单/持仓/余额**不受影响**，但提现权限和 IP 白名单要你自己去网页后台核对
  · 余额也不通 → 才是密钥或端点的问题，这时候才贴排查表
- 两种情况都会写明当时连的是哪个环境、哪个端点

## v1.26.2　(2026-08-18)
- 连着**实盘**时，启动播报会主动说一声「当前连的是 Bybit 实盘（真钱）」，
  并带上下单总开关的状态。以前只有发 /version 才看得到，
  而重启/改配置/部署都会重来一遍——真钱的事不该靠人记得去查
- 模拟盘不喊（喊多了就没人看了）

## v1.26.1　(2026-08-18)
- 新增端点探测：`docker compose exec crypto-bot python bybit_trade.py probe`
- 三个端点（testnet / 主站模拟交易 / 实盘）各查一次余额，直接告诉你这把 key
  属于哪一套、该把 BYBIT_TESTNET 设成什么。只读，不下任何单
- 三个都不认就不是端点的事，会列出剩下的可能：secret 抄错、key 没启用/已删/
  已过期、IP 白名单里不是这台服务器
- 探测完会把环境变量还原，不会因为试了一下就把运行环境改成实盘

## v1.26.0　(2026-08-18)
- 支持 Bybit 的**主站「模拟交易 Demo」**：`BYBIT_TESTNET=demo`
- 起因：配 key 时冒烟自测报 `401 API key is invalid`。Bybit 有**两套模拟盘**，
  key 互不通用、端点也不同——testnet.bybit.com 要单独注册走 api-testnet，
  主站右上角切的「模拟交易」走 api-demo。以前只支持前者，
  拿主站模拟交易的 key 去打 testnet 就是这个 401
- 交易所对这种情况只回一句「API key is invalid」，一个字都不提端点。
  现在 401 会直接列出三种可能（端点对不上／值里混了引号空格／key 没启用或过期），
  `/keycheck` 里也看得到，不用 SSH 上服务器
- 冒烟自测会先打印当前端点和 key 的位数——key 是空的就说明改完 .env 忘了 force-recreate
- 认不出来的 BYBIT_TESTNET 值仍然一律当模拟盘（防手滑上实盘）

## v1.25.3　(2026-08-18)
- 修：菜单里【标注图表】的说明写着「图上标 🟡EMA20 🔵EMA50 🟣EMA200」，
  而图上画的其实是 MA3/13/23——颜色对得上、名字全错。帮助里那条也一样
- 这类过期文案和错的代码一样会误导人，而且更难发现（没人会去测文案），
  所以加了护栏测试盯着说明和图上画的是不是同一套

## v1.25.2　(2026-08-18)
- 修：点【查链上代币】之后在群里发中文币名（比如「牛来」），**什么反应都没有**
- 原因是上一次治群里刷屏时用了一刀切的规矩：带中文的一律当闲聊静默放行。
  可中文名的币是真实存在的——「牛来」就是，这条把它一起吞了
- 现在收币名的引导态（链上查询、开仓选币、预警选币）认中文了；
  收「DOGE 5」这类参数行的仍然按原规矩卡死
- 放开中文不等于什么都收：长句、带标点、带空格、带口语虚词的照样当闲聊。
  当初刷屏的原话「又不能做网格」仍然拦得住
- 链上提示里补上更快的路：直接发 `/oc 牛来` 或 `/oc 0x地址`，不用先点按钮进来

## v1.25.1　(2026-08-18)
- 修：微市值扫描点「300万」「500万」是空的，只有「1000万」出东西——
  而 1000 万那张单子里明明躺着 200 万市值的币，自相矛盾
- 两个原因：① 市值表只翻到排名 1250 就被行情源限频截断了，而 300 万在 1831 名开外；
  ② 缓存的命中条件写错，等于没有缓存，每次扫都重拉一遍再被限一次频
- 现在市值表**后台每 6 小时建一次**（翻满 16 页 ≈ 排名 4000），命令直接读现成的；
  某一页被限频只跳过那一页，不再把后面十几页一起丢掉
- 实测修完：300万档 47 个、300~500万 32 个、500~1000万 71 个（原来 300万档是 0）
- 卡片加了**分档统计**：一眼看清每个档位各有几个，不用靠点进去猜
- 当前档位按钮打勾，刷新不再莫名其妙跳回 300 万
- 新增 `/oc` 两字母短命令查链上币价（`/oc BANK`），不用再点四五下按钮

## v1.25.0　(2026-08-18)
- 新增 💎 **微市值扫描** `/microcap`：四家交易所（现货+永续）里流通市值低于
  300 万、**而且真买得到**的币。按钮在「机会扫描」里，`/microcap 1000` 换上限
- 卡片上写明市值/排名、24h 量、哪几家能买、FDV 和流通率——低流通高 FDV 的
  直接标「解锁砸盘风险」，因为筛的是流通市值，不标出来等于埋雷
- 是从**交易所往回筛**，不是从市值榜往下捞：300 万落在第 1831 名，往下一万多个币
  九成没上任何交易所，捞上来也下不了单
- 首次真跑抓到三类假货，都已经堵上：
  · 代币化美股（QQQB/SOXSB/MRVLB 这些一度占了前十的一半）
  · 同名误判（币安的 UTK 配到了「UNITE THE KINGDOM」，市值 1 万却一天成交千万）
  · 成交额超市值 5 倍的一律剔除——那是配错币，不是发现宝藏
- 查不到市值的**不算命中**，单独计数；市值榜被限频少取了页数会在卡片上明说

## v1.24.3　(2026-08-18)
- 链上监控加了 **±5% 一档**（原来最低 10%）。四档改成每行两个，手机上不会被截断
- 修：机器人忙的时候点按钮，那一下**什么都不会发生**，管理员还收到一条报错
  （早上 9 点每日播报同时最容易撞上）。原因是「消掉按钮转圈」这一步失败，
  把后面真正干活的流程一起打断了。现在应答失败只记日志，操作照常执行
- ±5% 放开的前提是节流本来就在：5 分钟冷却、同方向同量级去重、
  15 分钟收盘确认（插针只标不当信号）、浅池建监控时直接警告

## v1.24.2　(2026-08-17)
- 上一版说「均线口径全局统一」其实只统一了一半，这版把漏掉的六处补齐：
  `/analyze`、`/ai`、`/chartanalyze` 的图、持仓驾驶舱、指标告警的金叉死叉、
  AI 的 BTC/ETH 联动——它们还在用 MA7/MA30 或 EMA20/50 说话
- **最要命的一条**：给 AI 的工具说明写着「已算好 EMA20/50/200」，实际给的是
  MA3/13/23 的值。AI 不会报错，只会把短均线当长均线解读，分析全歪
- `/analyze` 的均线现在也会说「缠绕，方向未定」，不再把缠绕硬算成看空多记一票
- 指标告警的金叉/死叉改看 MA3 与 MA23。换版当天不会凭空推一次金叉
  （旧状态是按老口径记的，已弃用不复用）
- 补了 7 条护栏测试盯着这六处——上次漏网就是因为护栏只盖了四个模块

## v1.24.1　(2026-08-17)
- 修：上一版换均线口径时把 `/scan` 的趋势判定改过严了——大半个市场被判成「分歧」，
  扫描几乎给不出绿灯。现在和破位扫描共用同一个判定函数
- 修：同一处改动漏了个变量，导致趋势一律显示「无数据」（异常被吞掉，不报错）

## v1.24.0　(2026-08-17)
- 均线口径**全局统一成 MA3/13/23**：日线蜡烛图（发币名自动出的那张）、AI 分析、
  技术分析文字、`/scan` 趋势判定，以前分别是 MA7/25/99、EMA20/50/200，三处对不上
- **图上能显示中文了**：链上代币「牛来」这种名字不再退成合约地址开头
- `/scan` 换到 OKX 不再一律 55 分封顶——拿它的按币种持仓量当代理指标，
  拥挤度算得出来了（以前 OKX 没有按合约的接口，只能标"未知"）

## v1.23.1　(2026-08-17)
- 修：点过菜单按钮没填完，之后在群里说的**每句话**都被当成"在回答提示"，
  机器人逐条回「请发「币 百分比」」——别人正常聊天被刷屏
- 现在群里带中文/长句的消息一律当闲聊**静默放行**，不再纠正
- 引导态只在**点按钮的那个会话**里生效（以前私聊点的会拦住你在群里的发言）
- 引导态 5 分钟自动失效，不用记得发 /menu 取消（以前永不过期，重启都还在）
- 真发「DOGE 5」这种参数行仍然照常接住

## v1.23.0　(2026-08-17)
- K 线均线全部换成 **MA3 / MA13 / MA23** 三根（交易所图和链上图统一）
- 新增 🚀 **5分钟破位扫描**：热门币里「箱体整理 → 放量破边界 → MA3/13/23 已顺势」的那一下
- 向上突破和向下跌破都报，每条都带**箱体另一侧的止损位**和距离
- 已默认订阅，自动推到群里（每 5 分钟扫一次）；不想要发 `/breakout off`
- 阈值是回放 23 个热门币 × 3.3 天的 5m 历史量出来的：
  松的那套一天响 56 次（刷屏），现在这套一天约 17 次、止损中位 1.33%
- 只报**破出去那一根**，不会价格在箱外飘着就一直响

## v1.22.1　(2026-08-17)
- 修：链上告警的「数据完整度」永远少一项——它根本没去查安全检查，是条必现的假缺失
- 现在告警会带上安全结论（有高危会直接列出来）
- 修：K线取不到时只会说「拿不到K线」。真因多半是行情源**限频**（实测连打第 3 次就被拒）
- 加了限流 + 退避重试 + 60 秒缓存：连着要 6 次现在 6 次都成功
- 取不到时会说清是限频、还是这个池子行情源没索引到

## v1.22.0　(2026-08-17)
- 🔗 链上代币升级成**首页独立专区**：查币、六条链热门、我的链上监控，一个入口全在
- `/scan` 全市场扫描终于也能选交易所了——这是最后一个还锁死 Bybit 的功能
- 扫描换所扫的是**那家的盘口和深度**，结果卡上写明数据源（换所不可比）
- 重要：某家给不出资金费率/持仓量时**不再给绿灯**——
  以前同一个币在 Bybit 是「拥挤度极高·不建议」，换到 OKX 就变成 86 分 ✅，
  缺数据反而让它看起来更安全
- 盘口深度按各家单位归一（OKX/Gate 的档位是「张」不是「币」，不换算会差几百倍）

## v1.21.0　(2026-08-17)
- 新增合约安全检查：买卖税、能不能卖出去、合约权限、LP 锁仓、持仓集中度
- 详情卡上一个按钮就能查，六条链都支持
- **查不到的项会明说"查不到 ≠ 安全"**，绝不把"没说有问题"当成"没问题"
- 权限存在但所有权已放弃的，会说清"目前没人能调用"，不再一律吓唬
- 池子太浅／假池／多池差价过大／数据残缺／合约高危时，**暂停交易型信号**，只发风险提示
- 链上告警现在带流动性、$1k/$5k 预估滑点和数据完整度
- 链上告警加了收盘确认：瞬时插针不再当信号（照发但标「未确认」且不重设基准）
- 链上冷却拉长到 5 分钟，同方向同量级去重
- 池子被撤走导致连续取不到价时，监控会标为失效并通知一次，不再假装还在盯

## v1.20.0　(2026-08-17)
- 链上代币现在用「链 + 合约地址 + 池地址」唯一识别，卡片上三个都写出来
- 价格标明来自哪个 DEX 的哪个池、该池多深、什么时候取的数
- 新增多池价格偏差：同一个币不同池子差多少一目了然，超 3% 会提醒你成交前先确认走哪个池
- K 线图标出**插针**（紫✕）和**异常放量**（橙▲），最后一根没收盘会明说
- 插针的判定用这个币自己的波动做基准，不是固定百分比——否则 200 根里能标出 48 根，等于没标

## v1.19.0　(2026-08-17)
- 链上的币现在能监控了：查完直接点【🔔 涨跌±10%/20%/50% 提醒】，不用记命令
- 命令也行：`/watchpct <合约地址> 20`，BNB链/以太坊/Solana 等六条链都支持
- 价格预警同样认合约地址：`/alert <合约地址> 0.08 above`
- 链上默认阈值比交易所高一档（±10% 起）——池子浅，±5% 一天能响几十次
- 建监控时会先验一遍：地址查不到就不建，免得你以为在盯其实永远不会触发
- 池子太浅会当场警告：单笔大单就能打出假信号

## v1.18.1　(2026-08-17)
- 链上搜索结果现在**每条都能点进去**看详情，不用再手工复制合约地址重发一遍
- 详情卡把「当前价」放到最显眼的位置（原来夹在中间，一眼看不到）
- 加了**链上 K 线图**：15m / 1h / 4h / 1d 四个周期，带 EMA20/50 和成交量
- 新池子在大周期上凑不够 K 线时，会告诉你换**更小**的周期（原来提示说反了）

## v1.18.0　(2026-08-17)
- 新增 `/onchain`：查**链上**代币，交易所还没上的那些币（币安钱包/Alpha 那一段）
- 直接把合约地址发给我就能查，不用打命令
- 支持 BNB链 / 以太坊 / Solana / Base / Arbitrum / 波场，各链有真实热门榜
- 同名假币是链上常态（搜 PEPE 出 30 个），所以一律按「可信流动性」排序并给出合约地址
- 假池会被揪出来：标称流动性可以用垃圾币配对造，成交量造不了——
  实测有个池子标称 2.2 亿，24h 只成交 4 美元
- 每个结果都标风险：池子太浅、新池、市值虚高、疑似刷量、卖出异常少、24h暴涨
- 热门榜用的是真实交易热度，不是付费推广位（那种榜单本质是广告）

## v1.17.1　(2026-08-15)
- 修了个一直在漏币的老毛病：Bybit 有个「创新区」标记，我以前把它当成代币化股票，
  于是 120 个新上的小币（AKE、AEON、ACU 这些）**从来没进过扫描池**
- 修完 Bybit 的可扫币从 395 个涨到 515 个，`/scan` 候选池里也能看到 AKE 了
- 缓步增长 `/steady` 也能选交易所了：Gate 的币池比 Bybit 大一截，小币缓涨往往只在那边扫得出来
- 代币化股票现在四家一起认：OKX 自己不标品类，靠另外三家标注过的名字反查
  （SNDK 那种成交额排全所第二的美股，不剔掉会占满第一页）

## v1.17.0　(2026-08-15)
- K 线类功能也能选交易所了：技术分析、AI 行情工具、标注图表、规则回测、条件提醒
- 之前只有取价能选，看图和分析还是只认 Bybit —— 现在跟 `/source` 的设置走
- 小币终于能看图了：AKE、TUT 这类 Bybit 没有、Gate 有的，换个源就能画
- 每张图、每份分析、每次回测都标注数据源，换了所一眼能看出来
- OKX 单次只给 300 根 K 线，取不满会如实写明，不假装按你要的根数算
- 条件提醒每条记住自己的源，不会今天用这家明天用那家来回误触发
- 还没跟着走的：全市场扫描 `/scan` `/steady` 仍是 Bybit（下一步）

## v1.16.8　(2026-08-14)
- 修复点「🔄 刷新」时数据没变会给管理员推一条"机器人异常"的问题，刷新出一样的内容本来就不是错误
- 顺带修好一类隐患：面板里的币名带下划线等符号时可能整条消息发不出去，现在会自动降级成纯文本

## v1.16.7　(2026-08-14)
- 修正合约概览取错交易所：以前可能返回 OKX 数据，而同一轮的K线/OI/盘口全是 Bybit，两边价格和资金费并不一样
- 数据状态改成分层显示（市场数据 / 账户数据 / 缺失维度），不再只给一个"N/M 项可用"
- 各项数据取数间隔过大时会提示"非同一时点快照"，盘口和几分钟前的K线不该当作同时发生
- 盘口与逐笔成交方向打架时，AI 必须标明"信号冲突"，不能挑一边给单边结论

## v1.16.6　(2026-08-14)
- 分析结果底下加「进交易台」按钮，看完分析可以直接去下单，不用再手工翻菜单
- AI 回复带上下文时会给「换币种」「清除上下文」两个按钮，不用记 /resetchat
- 新增 /btcregime：BTC 4h 均线排列换状态时提醒你回看计划和仓位，带防抖不刷屏

## v1.16.5　(2026-08-14)
- 版本更新现在会播报到群里，不再只发管理员私聊
- 只在版本号真的变了时才播报，容器重启、改配置不会刷群
- 顺带修好：群升级成超级群时，急涨急跌订阅、周报订阅、合约告警档位以前不会跟着搬家

## v1.16.4　(2026-08-14)
- 群里回复告警、播报时不再误触发 AI 应答 —— 现在只有回复 AI 自己说过的话才算在跟它说话
- 回复 AI 那条但正文 @ 的是别人时，机器人闭嘴，不再插进你和同事的对话
- @机器人 提问、以及回复 AI 继续多轮追问，都和原来一样

## v1.16.3　(2026-08-14)
- 部署触发失败时说清是「等一会重试」还是「去改配置」，不再只甩一句 HTTP 503
- 失败卡明确写出：这一步没触发成功，服务器代码没动、线上仍是旧版本，重试是安全的

## v1.16.2　(2026-08-14)
- 启动播报现在会带上这一版改了什么（就是你正在看的这段）
- 新增 `/changelog`：看任意版本的更新说明，帮助面板里也有按钮
- 发版时必须写更新说明，否则测试不过、部署直接中止

## v1.16.1　(2026-08-14)
- 修：菜单点进去的【👁 持续波动监控】漏了选交易所（只有 `/watchpct` 命令支持）
- 引导面板现在显示当前数据源，并带按钮直接改
- 【我的波动监控】【我的价格预警】每条都能单独换源，不用删了重建

## v1.16.0　(2026-08-14)
- 取价类功能可以选交易所了：`/source` 设默认，每条预警/监控还能单独覆盖
- 可选 OKX / 币安 / Bybit / Gate × 现货 / 永续
- 修：价格预警以前卡主流币白名单，AKE、TUT 这类小币根本设不了——现在能了
- 每条预警按自己的源取价，绝不拿 A 所的基准比 B 所的现价（换源必定重设基准）
- 指定了交易所就不偷偷换家：查不到会如实说「这家没有这个币」
- 提速：取价不再每次新建 SSL 连接（原来光建连接就 0.77 秒）
- 不跟着换的：实盘只能 Bybit；虚拟合约固定 Bybit 永续；扫描/图表/回测仍是 Bybit

## v1.15.0　(2026-08-14)
- 新增 🟢 Gate 专区，币安专区的七个功能一比一复制
- 爆仓这里有真数据（币安已关公开接口，那个按钮只能提示去 OKX 看）
- 多空比多给了大户账户比，散户与大户背离时才有信息量
- 榜单剔除杠杆代币 3L/3S 与代币化股票（Gate 上这两类占了一大半）

## v1.14.1　(2026-08-14)
- 修：【🌱 缓步增长】按钮点了没任何反应——回调没接线，点下去既不报错也不动
- 加了泛化守卫：以后有按钮没接线，测试会先炸

## v1.14.0　(2026-08-13)
- 缓步增长：7天窗口 + 短窗口用4h

## v1.13.1　(2026-08-13)
- 缓步增长：菜单加天数切换

## v1.13.0　(2026-08-13)
- 缓步增长扫描 + symbolType 识别代币化股票

## v1.12.1　(2026-08-13)
- 扫描器：分歧不给绿灯 + 极端费率与拥挤方向否决

## v1.12.0　(2026-08-12)
- 取价兜底 + 健康检查降噪

## v1.11.1　(2026-08-08)
- 修扫描器流动性评分与深度指标

## v1.11.0　(2026-08-08)
- 全部命令按钮面板

## v1.10.1　(2026-08-08)
- 首页恢复全平铺

## v1.10.0　(2026-08-08)
- 虚拟盘：小币可练 + 复盘诊断接入

## v1.9.0　(2026-08-08)
- AI外发账户数据脱敏

## v1.8.2　(2026-08-08)
- 去掉中文斜体

## v1.8.1　(2026-08-08)
- 修双实例：部署清理漏网 + 409自动告警

## v1.8.0　(2026-08-08)
- 首页精简 + 分析闭环 + 确认页成本

## v1.7.0　(2026-08-07)
- 备份恢复 + 系统体检

## v1.6.1　(2026-08-07)
- 修部署覆盖生产data.json

## v1.6.0　(2026-08-07)
- 补齐遗漏五项：菜单入口/计划vs成交/扫描过滤/虚拟盘同构/密钥安全

## v1.5.0　(2026-08-07)
- P3: 规则回测/个性化风控参数/周报行为画像

## v1.4.0　(2026-08-07)
- P2: 扫描器/流动性分布/亏损归因/虚拟盘同构/事件预警

## v1.3.0　(2026-08-07)
- P1: 净盈亏比/计划取消条件/组合风险/会话上下文

## v1.2.1　(2026-08-07)
- 播报行情源降级 + 失败原因可见

## v1.2.0　(2026-08-07)
- P0: 数据可信度 + 合约身份 + 真实规格风控

## v1.1.0　(2026-08-07)
- 合约告警去重根因修复 + AI降级覆盖超时

## v1.0.99　(2026-07-30)
- fix(deploy): 部署前清掉残留容器，根治「有的命令回、有的不回」的双实例抢token

## v1.0.98　(2026-07-30)
- 急涨急跌更容易命中——成交额门槛300万→100万U + 热身/运行时长可见

## v1.0.97　(2026-07-29)
- /alertnow 立即补推 + /alertdiag 自检——把「怎么没告警」变成看得见

## v1.0.96　(2026-07-29)
- 合约异动告警按钮面板 /contract（含每群最低档过滤）

## v1.0.95　(2026-07-29)
- 急涨急跌按钮面板 /pump（阈值不写死，点按钮调）

## v1.0.94　(2026-07-29)
- pumpalert 测试隔离，修部署被 flaky 测试卡住的真因

## v1.0.93　(2026-07-29)
- bump v1.0.93（重新部署，代码同 v1.0.92，上次为健康检查误报）

## v1.0.92　(2026-07-29)
- /pumptop 15m滚动涨跌榜（急涨急跌自检，解决「没反应」的困惑）

## v1.0.91　(2026-07-29)
- Bybit 全永续「15m 急涨急跌」告警（/watchpump）

## v1.0.90　(2026-07-23)
- 能读用户上传的 JSON/CSV/日志文件（服务端解析 + AI 按需查询）

## v1.0.89　(2026-07-23)
- AI 模型掉渠道时自动降级到备用模型（修 404 罢工）

## v1.0.88　(2026-07-23)
- AI 支持看图（发截图机器人能直接读了）

## v1.0.87　(2026-07-18)
- 持仓驾驶舱 /cockpit——逐仓状态+建议动作+账户红色风险

## v1.0.86　(2026-07-18)
- 计划预填到交易台开仓 /plan → 确认卡（补完 spec #4 闭环）

## v1.0.85　(2026-07-18)
- 交易计划 /plan——分析到执行的闭环脊柱（覆盖 spec #2/#5 + #4 半）

## v1.0.84　(2026-07-17)
- 风险反推仓位 /risk 入场 止损 风险%

## v1.0.83　(2026-07-17)
- 数据可信度层——数据时间 + 完整度 + 异常显式降级

## v1.0.82　(2026-07-17)
- 标注图表 /achart + 持仓币新闻优先

## v1.0.81　(2026-07-17)
- 条件触发提醒 /cond + 资金费率极端榜 /fex

## v1.0.80　(2026-07-17)
- 实盘复盘 /rstats + 风险守护 /risk + AI盘前简报 /brief

## v1.0.79　(2026-07-17)
- test/fix/perf: 单元测试接入流水线 + 合约分档根因修复 + data.json 增长治理

## v1.0.78　(2026-07-17)
- 部署提速(用Dockerfile预装依赖) + 虚拟盘改用Bybit永续价 + AI限流 + AI查虚拟仓

## v1.0.77　(2026-07-17)
- docker-compose 漏传 BYBIT_* 环境变量(实盘功能一直是死的) + 全局错误上报

## v1.0.76　(2026-07-17)
- README/OPERATIONS 全面更新到当前功能与运维实况(距上次更新2周)

## v1.0.75　(2026-07-17)
- 去掉部署卡片/version 里的斜体(下划线Markdown)，改成正常字体

## v1.0.74　(2026-07-17)
- /version 运行状态自查（版本/AI模型/Bybit/管理员）+ 启动播报带版本号

## v1.0.73　(2026-07-17)
- AI 量化数据全家桶——多周期K线/OI历史/资金费+基差/订单簿/逐笔/联动/账户

## v1.0.72　(2026-07-17)
- 群升级为超级群时自动迁移订阅 + /migratechat 手动补救

## v1.0.71　(2026-07-17)
- 加状态持久化(PicklePersistence)——按钮引导流程不再因重启/部署失效

## v1.0.70　(2026-07-17)
- 群内引导流程(设监控/预警)被AI对话抢占——回复机器人提示时收不到输入

## v1.0.69　(2026-07-17)
- /id 增加"是否管理员+当前生效的管理员id列表"，排查部署权限

## v1.0.68　(2026-07-17)
- 部署触发HTTP303误判失败(重定向=已触发) + 去掉用户可见的"Jenkins"字样

## v1.0.67　(2026-07-17)
- ADMIN_CHAT_ID 支持多个管理员id(逗号分隔)

## v1.0.66　(2026-07-17)
- /whois <user_id> 把 Telegram user_id 解析成名字/用户名(私聊)

## v1.0.65　(2026-07-17)
- AI没指定币时默认BTC主动查数据 + 按钮改为连续问答会话

## v1.0.64　(2026-07-17)
- AI回复不引用原消息(直接发群) + 菜单加💬AI助手按钮 + markdown渲染修复

## v1.0.63　(2026-07-17)
- 查币价失效——群聊AI handler吃掉所有文字消息(回归)

## v1.0.62　(2026-07-17)
- 群聊AI升级为工具调用版(能查实时数据)+放开系统提示

## v1.0.61　(2026-07-17)
- 群内@机器人自由对话(AI, 多轮上下文) + /ask + /resetchat

## v1.0.60　(2026-07-15)
- 合约异动告警刷屏——同币同档加6h推送冷却兜底

## v1.0.59　(2026-07-15)
- 实盘交易台 /trade + 引导式开仓 + 持仓一键操作按钮(零记忆)

## v1.0.58　(2026-07-15)
- 实盘 /rtpsl 改止盈止损 + /rliqalert 爆仓临近预警

## v1.0.57　(2026-07-15)
- Bybit实盘手动交易 /ropen /rclose /rpos /rbal /rorders /rcancel

## v1.0.56　(2026-07-14)
- 虚拟交易回复改用 safe_reply(Markdown失败降级纯文本), 杜绝静默无回复

## v1.0.55　(2026-07-14)
- 虚拟合约交易(模拟盘) /vopen /vclose /vpos /vhistory

## v1.0.54　(2026-07-14)
- 连涨/连跌扫描 + 检查清单接入菜单按钮(策略回测分类)

## v1.0.53　(2026-07-14)
- /downstreak 连续N天下跌合约扫描(抄底参考)

## v1.0.52　(2026-07-14)
- /help 补充盯盘/合约新功能(watchpct/upstreak/watchcontract/checklist/费率周期)

## v1.0.51　(2026-07-14)
- 合约涨跌榜补上跌幅榜(OKX/币安/Bybit)

## v1.0.50　(2026-07-14)
- /upstreak 支持 Bybit + 多交易所(OKX/Bybit/all)

## v1.0.49　(2026-07-14)
- /upstreak 连续N天上涨合约扫描(OKX永续,日线收阳)

## v1.0.48　(2026-07-14)
- 波动监控接入 WebSocket 秒级实时(OKX/Bybit永续)

## v1.0.47　(2026-07-13)
- 合约卡显示资金费结算周期(1h/8h高频吸血提醒) + /checklist 命令 + 合约告警跨所去重

## v1.0.46　(2026-07-13)
- 波动监控锁定基准交易所，杜绝跨所比价算歪

## v1.0.45　(2026-07-13)
- /watchpct 兼容粘贴完整交易对名(TUSDT→T, BTCUSDT→BTC)

## v1.0.44　(2026-07-13)
- 波动监控支持强制盯合约价 /watchpct 币 % 合约

## v1.0.43　(2026-07-13)
- 波动监控接入按钮菜单(价格预警分类)

## v1.0.42　(2026-07-13)
- 持续波动监控 /watchpct（指定币±%反复提醒，支持小盘/合约币）

## v1.0.41　(2026-07-13)
- 合约告警刷屏——20%边界抖动重复报 + 同币重复行

## v1.0.40　(2026-07-13)
- 单字母币(T/B)可查价修复 + Bybit永续实盘网格模块(默认模拟盘) + 菜单加网格命令

## v1.0.39　(2026-07-09)
- 合约异动扫描间隔 3分钟→5分钟(降频)

## v1.0.38　(2026-07-09)
- 市场异动告警去掉现货涨跌幅分级,改为全交易所新币+放量

## v1.0.37　(2026-07-09)
- 全交易所合约涨跌幅分级告警(20%~400%)

## v1.0.36　(2026-07-08)
- 验证 CI 自动触发（空提交）

## v1.0.35　(2026-07-08)
- /momentum 稳定性——日线1小时长效缓存+分批重试，结果可复现不再漂移

## v1.0.34　(2026-07-04)
- /momentum 稳定性——日线1小时长效缓存+分批重试，结果可复现不再漂移

## v1.0.33　(2026-07-04)
- /menu 新增「策略回测」分类，弱势扫描/动量回测可点按钮直达

## v1.0.32　(2026-07-04)
- /weak 行为过滤挂钩美元资产，横盘榜不再被$1稳定币/代币化美债霸榜

## v1.0.31　(2026-07-04)
- /momentum 逐币容错，限流(429)时拿到几个算几个不再整体失败

## v1.0.30　(2026-07-04)
- /weak /momentum 增强容错，_get 加 429 退避重试

## v1.0.29　(2026-07-04)
- 新增 /weak 弱势横盘扫描 与 /momentum 动量轮动回测命令

## v1.0.28　(2026-07-04)
- 地址追踪阈值增加高档位(100万/150万/300万)

## v1.0.27　(2026-07-03)
- 信息卡大幅扩充(24h高低/ATH/供应量FDV/合约深度/情绪/关键位)

## v1.0.26　(2026-07-03)
- 买卖估算改振幅拆分 + 综合信号缩量降级 + 均线三色分开

## v1.0.25　(2026-07-03)
- 查币名结果直接发群里,不引用用户消息(do_quote=False)

## v1.0.24　(2026-07-03)
- RSI 改用 Wilder 平滑法(与交易所/TradingView口径一致)

## v1.0.23　(2026-07-03)
- 查币名全自动推送完整信息(信息卡+蜡烛图研判)

## v1.0.22　(2026-07-02)
- 新增 Bybit 专区 + 技术指标告警(RSI/均线)

## v1.0.21　(2026-07-01)
- 地址追踪默认阈值改为 >="

## v1.0.20　(2026-07-01)
- 地址追踪加最小金额过滤(默认≥\,稳定币/ETH折算美元),面板按钮可调,滤掉热钱包小额噪音

## v1.0.19　(2026-07-01)
- Gas提醒/套利监控/地址追踪 全部按钮化(状态显示+预设阈值+地址增删),工具菜单重排

## v1.0.18　(2026-07-01)
- 巨鲸地址追踪(/track /untrack /tracked, Etherscan, ETH+代币转账后台告警); README更新

## v1.0.17　(2026-07-01)
- Gas阈值提醒(多链gas+订阅+后台告警) + 套利监控(扣手续费净价差+多所+订阅+扫描告警)

## v1.0.16　(2026-07-01)
- 新增运维手册 OPERATIONS.md(巡检/更新回滚/日志/故障排查/密钥轮换)

## v1.0.15　(2026-07-01)
- 新增 README(功能/结构/部署/配置/CI审批流程/回滚/数据源)

## v1.0.14　(2026-07-01)
- 部署任务完成后把结果(成功/失败)回传到审批群

## v1.0.13　(2026-07-01)
- 部署触发改用 Jenkins 用户API Token(Basic认证),解决403; compose透传

## v1.0.12　(2026-07-01)
- 触发失败保留重试按钮

## v1.0.11　(2026-07-01)
- 传入 JENKINS_URL/JOB/DEPLOY_TOKEN 给容器(否则机器人读不到)

## v1.0.10　(2026-07-01)
- 加安全诊断(token长度+前11位)

## v1.0.9　(2026-07-01)
- set +x 关闭回显,避免token泄露到日志

## v1.0.8　(2026-07-01)
- 去凭据空白+改JSON body发送+打印HTTP状态

## v1.0.7　(2026-07-01)
- 测试审批部署链路(webhook->通知->确认->部署)

## v1.0.6　(2026-07-01)
- 显示当前会话 chat_id 和 user_id(配置审批群用)

## v1.0.5　(2026-07-01)
- Telegram按钮确认后机器人远程触发Jenkins部署; 新增Jenkinsfile.notify(webhook通知)

## v1.0.4　(2026-07-01)
- 涨幅榜OKX/币安都提到Top15; 查币价现货也标来源(CoinGecko/OKX/币安)

## v1.0.3　(2026-07-01)
- 新增币安专区(镜像OKX)+OKX查不到自动回退币安并标注来源; quickprice三级回退CoinGecko/OKX/币安

## v1.0.2　(2026-07-01)
- 群里查价:对像币代码的输入也给'没查到'提示,过滤中文/普通聊天/超短词

## v1.0.1　(2026-07-01)
- Jenkinsfile: 改为按版本tag部署/回滚(tag由推代码时生成)

## v1.0.0　(2026-07-01)
- Jenkinsfile: 语义化版本 vX.Y.Z, 自动+patch并推tag到GitHub, 支持按tag回滚
